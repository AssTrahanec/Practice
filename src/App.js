import React from 'react';
import { AuthProvider } from './AuthProvider';
import AppRouter from './AppRouter';
const App = () => {
    return (
            <AppRouter />
    );
}

export default App;
